#!/usr/bin/env sh
set -eu

message_file="${1:-}"
if [ -z "$message_file" ]; then
  exit 0
fi

node scripts/updates/prepare-client-update.mjs --pending-message-file "$message_file"
git add build/client-updates
